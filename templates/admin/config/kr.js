const API = require(`../../config/${process.env.TYPE}_kr`)
const OSS_BASE = process.env.OSS_BASE

let TRADE, DOMAIN
switch (process.env.TYPE) {
case 'dev':
  break

case 'prod':
  break

case 'pre':
  break

}

/**
 * kr节点
 */
module.exports = {
 

}