let TRADE, DOMAIN
const API = require(`../../config/${process.env.TYPE}_en`)
const OSS_BASE = process.env.OSS_BASE

switch (process.env.TYPE) {
case 'dev':
  break

case 'prod':
  break

case 'pre':
  break

}

/**
 * en节点
 */
module.exports = {
 
}