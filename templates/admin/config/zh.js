let TRADE, DOMAIN

const { USER_URL } = require(`../../config/${process.env.TYPE}_${process.env.LANG}.js`)
const OSS_BASE = process.env.OSS_BASE
switch (process.env.TYPE) {
case 'dev':
  break

case 'test':
  break

case 'prod':
  break

case 'pre':
  break

}

/**
 * 国内节点
 */
module.exports = {
 

}